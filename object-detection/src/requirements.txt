# pyneat 0.3.0 requires numpy<2. Leaving this unpinned makes pip upgrade to
# numpy 2.x, which breaks pyneat and every simaai-* package on the DevKit.
numpy>=1.24,<2

# The DevKit image already ships opencv via /usr/lib/python3/dist-packages, and
# app.py adds that path at startup. Install this only if the system one is
# missing; opencv-python 5.x pulls numpy 2.x as a dependency.
opencv-python>=4.7,<5

pyyaml>=6.0
